{
  "type": "object",
  "$schema": "http://json-schema.org/draft-04/schema#",
  "properties": {
    "amount_spent": { "type": "number", "$comment": "minimum amount to be spent to get points" },
    "points_given": { "type": "integer", "$comment": "points given for above mentioned amount spent" },
    "reward_point": { "type": "integer", "$comment": "Number of reward points to claim the reward" },
    "reward_detail": { "type": "string", "$comment": "Ex: Free Milkshake" }
  },
  "required": ["amount_spent", "points_given", "reward_point", "reward_detail"]
}